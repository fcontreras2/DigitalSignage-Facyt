{%- block form_errors -%}
{%- if errors|length > 0 -%}
    
        {%- for error in errors -%}
            <span style="color:#DB2929">{{ error.message }}</span>
        {%- endfor -%}
    
{%- endif -%}
{%- endblock form_errors -%}
